#!/usr/bin/env python
#coding:utf-8
from scapy.all import *
print(dir(IP.layers))
print(dir(IP.layers))
print(dir(IP.layers))
# ping_one_reply = sr1(all.IP(dst = '192.168.18.2')/all.ICMP(),timeout = 1)
# ping_one_reply.show()



